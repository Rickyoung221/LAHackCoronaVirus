{
  "name": "lahack-coronavirus",
  "private": true,
  "scripts": {
    "build": "node scripts/build-static.js",
    "dev": "npm run build && concurrently -k -n build,serve -c blue,magenta \"nodemon\" \"browser-sync start --server dist --files dist --port 3000 --no-notify --no-open\"",
    "preview": "npm run build && python3 -m http.server 8080 --directory dist",
    "serve:dist": "python3 -m http.server 8080 --directory dist"
  },
  "devDependencies": {
    "browser-sync": "^3.0.3",
    "concurrently": "^9.1.2",
    "nodemon": "^3.1.9"
  }
}
